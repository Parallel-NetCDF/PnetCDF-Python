# Follow instructions below for editing.
[options]
# path to pnetcdf_config script (use if not found in unix PATH).
pnetcdf_config=/usr/bin/pnetcdf-config

[check-manifest]
ignore =
    .gitignore
    docs/*
    examples/*py
